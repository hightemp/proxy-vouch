{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "ProxyVouch",
  "version": "../package.json",
  "identifier": "dev.hightemp.proxypulse",
  "build": {
    "beforeDevCommand": "pnpm dev",
    "devUrl": "http://127.0.0.1:1420",
    "beforeBuildCommand": "pnpm build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "label": "main",
        "title": "ProxyVouch",
        "width": 1320,
        "height": 860,
        "minWidth": 1000,
        "minHeight": 650,
        "dragDropEnabled": false
      }
    ],
    "security": {
      "csp": "default-src 'self'; connect-src ipc: http://ipc.localhost; img-src 'self' asset: http://asset.localhost data:; style-src 'self' 'unsafe-inline'; script-src 'self'; object-src 'none'; base-uri 'self'"
    }
  },
  "bundle": {
    "active": true,
    "targets": ["deb", "appimage"],
    "category": "DeveloperTool",
    "shortDescription": "Check, organize and export your proxies",
    "longDescription": "A local desktop proxy checker. Import mixed-format lists, detect protocols, verify requests and export working or failed proxies.",
    "icon": ["icons/icon.png", "icons/icon.ico", "icons/icon.icns"],
    "linux": { "deb": { "depends": ["libwebkit2gtk-4.1-0", "libcurl4"] } }
  }
}
